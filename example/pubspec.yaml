name: whiteboard_sdk_flutter_example
description: A example of whiteboard_sdk_flutter

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  # When depending on this package from a real application you should use:
  # whiteboard_sdk_flutter: ^0.1.2
  whiteboard_sdk_flutter:
    path: ../

  cupertino_icons: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
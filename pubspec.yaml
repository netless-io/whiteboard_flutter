name: whiteboard_sdk_flutter
description: A flutter wrapper for agora whiteboard sdk.
version: 0.4.0+1
homepage: https://github.com/netless-io/Whiteboard-Flutter

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.22.2"

dependencies:
  flutter_inappwebview: ^5.3.2
  webview_flutter: ^3.0.0

  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.1.0
  build_runner: ^2.1.8
  flutter_lints: ^1.0.4

flutter:
  assets:
    - assets/whiteboardBridge/